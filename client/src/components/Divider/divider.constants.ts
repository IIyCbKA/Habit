import React from "react";

export const enum DIVIDER_ORIENTATIONS {
  horizontal,
  vertical,
}

export const enum DIVIDER_VARIANTS {
  fullWidth,
  inset,
  middle,
}

export const HR_ELEMENT: React.ElementType = "hr";
export const DIV_ELEMENT: React.ElementType = "div";
