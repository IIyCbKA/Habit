/*
--------------ButtonType enum--------------
The enum ButtonType specifies type (a set of styles) of button.
By default, type is Text.
*/
export const enum ButtonType {
  Text,
  Contained,
  Outlined,
}
