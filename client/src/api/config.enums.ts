export const enum ENDPOINT {
  LOGIN = "/auth/login/",
  REGISTER = "/auth/register/",
  REFRESH = "/auth/refresh/",
  LOGOUT = "/auth/logout/",
}

export const enum HTTP_STATUS {
  UNAUTHORIZED = 401,
}
